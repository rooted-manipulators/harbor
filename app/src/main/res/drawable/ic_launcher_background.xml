<?xml version="1.0" encoding="utf-8"?>
<!--
     The flat grey the icon art sat on before the background layer was cut
     back out of it, sampled from the source rather than guessed: see
     tools/cut_icon.py, which prints it on every run. Full bleed, because
     the mask is the system's job, not this layer's.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#D9D9D9"
        android:pathData="M0,0h108v108h-108z" />
</vector>
